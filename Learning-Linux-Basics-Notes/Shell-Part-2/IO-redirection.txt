IO-redirection:
------------------
IO Redirections:


Standards stream ka concept hota hain Linux main.:

Standard Stream:

three data streams Create hoti hain, jab hum Linux ka ek command run karte hain.

STANDARD INPUT

STANDARD OUTPUT

STANDARD ERROR

STANDARD INPUT: Stdin. standard input string, hain ye text as input accept karta hain.

Standard output: stdout
output aata hain hume command ko vo hume standard output stream deta hain.

Kabhi kabhi error bhi aata hain.. wrong command or command failed.

standard error: 
standard error stream  hume error bhejta hain.

IO redirection se hum standard input, standard output, standard error ko hum text file m redirect kr skte hain mtlb text file m le skte hain.
----------------------------------------------------------------------------------------------------------------------------------------------
mtlb command run karenge and uska output text file m lenge..

ls -lrt
du -h

ye dono command ka output hum text file m le skte hain and send kar skte haain..
----------------------------------------------------------------------------------------------------------------------------------------------
Jo output m hume screen par print nahi krna hain
Redirect standard output file m krne k liye:

forward arrow symbol uske bad file name use karenge..

echo $SHELL > shell.text

forward arrow kya karta hain, forward arrow shell.txt file na content ko overwrite karega standard output stream se
-------------------------------------------------------------------------------------------------------------------------------
support file shell.text m /bin/bash content already hain and hum overwrite nhi karna hain..

so hum double forward arrow use kr skte hain..

echo "This is the Bash Shell" >> shell.txt
ye already exist txt file m append kar dega.. overwrite nahi karega..

cat shell.txt
/bin/bash
This is the Bash shell
------------------
Suppose hum bas error msg redirect krna hain file main, ye error lena hain file main.
so hume use karna parega number 2 followed by forward arrow and file name
if the file does not exist, so vo new file create kar dega.
file already exist hain vo file ko overwrite karega

example: cat missing_file 2> error.txt
cat error.txt
cat: missing_file: No such file or directory
------------------
ab ap bologe ki mere pass file hain already, overwrite nahi krna hain..

so hum double forward arrow use karenge right
>>

exmaple: cat missing_file 2>> shell.txt

cat shell.txt
/bin/bash
This is the Bash shell.
cat: missing_file: No such file or directory
----------------------------------------------------------------------------error msg hume display nahi krna hain------------------------------
cat missing_file 2> /dev/null

Hume command execute karna hain par error print nahi karna hain screen par even if vo standard error generate karrta hain..
to hum /dev/null ko redirect kar skte hain..

/dev/null is called bit bucket / null device ek special device file
error ko discard karna hain so hum us karenge /dev/null

because hum /dev/null m kuch likh rahe to vo delete karta hain immediately
or read kar rehae /dev/null ko bhi to vo return nothing kuch return nahi krega..

-----------------------------------------------------------------------------------------------------------------------------------------------
cat logfile.txt
this is log file

log file k content ko delete karna hain without deleting file..
Clears the file without deleting it.

cat /dev/null > logfile.txt
