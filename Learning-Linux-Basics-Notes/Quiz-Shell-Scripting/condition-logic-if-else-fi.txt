Q1.  
Add a conditional statement at the end to check if status variable is set to failed. If it is run the rocket-debug command.
Ans:
mission_name=$1
rocket_status=$(rocket-status $mission_name)

echo "The status of launch is $rocket_status"

if [ $rocket_status = "failed" ]
then
  rocket-debug $mission_name
fi

Q2. Create a shell script in the home directory called check_dir.sh. 
The script should print the line Directory exists if the directory /home/michael/asia exists. 
If not, it should print Directory not found
Ans:
----------------------------------------check_dir.sh---------------------------------------------
if [ -d "/home/bob/caleston" ]
then
  echo "Directory exists"
else
  echo "Directory not found"
fi
---------------------------------------------------------------------------------------
Output:
chmod +x check_dir.sh 
./check_dir.sh 
Directory not found

Q3. Create a shell script in the home directory called check_greater.sh. 
The script should check the greater of the two command line arguments $1 and $2 and print which ever is greater.
Ans:
if [ $1 -gt $2 ]
then
    echo $1
else
    echo $2
fi

How to run:
chmod +x ~/check_greater.sh
./check_greater.sh 5 3
output:
5

Q4.Develop a shell script /home/bob/print-month-name.sh that accepts the number of a month as input and prints the 
name of the respective month. eg ./print-month-name.sh 1 should print January and ./print-month-name.sh 5 should print May. 
Also keep these in mind.
The script must accept a month number as a command line argument.
If a month number is not provided as command line argument, the script must exit with the message No month number given.
The script must not accept a value other than 1 to 12. If not the script must exit with the error Invalid month number given.

Ans:

month_number=$1

if [ -z $month_number ]
then
  echo "No month number given. Please enter a month number as a command line argument."
  echo "eg: ./print-month-number 5"
  exit
fi

if [[ $month_number -lt 1 ]] || [[ $month_number -gt 12 ]]
then
  echo "Invalid month number given. Please enter a valid number - 1 to 12."
  exit
fi

if [ $month_number -eq 1 ]
then
  echo "January"
elif [ $month_number -eq 2 ]
then
  echo "February"
elif [ $month_number -eq 3 ]
then
  echo "March"
elif [ $month_number -eq 4 ]
then
  echo "April"
elif [ $month_number -eq 5 ]
then
  echo "May"
elif [ $month_number -eq 6 ]
then
  echo "June"
elif [ $month_number -eq 7 ]
then
  echo "July"
elif [ $month_number -eq 8 ]
then
  echo "August"
elif [ $month_number -eq 9 ]
then
  echo "September"
elif [ $month_number -eq 10 ]
then
  echo "October"
elif [ $month_number -eq 11 ]
then
  echo "November"
elif [ $month_number -eq 12 ]
then
  echo December
fi
---------------------------------------------------
Q5.Write a script to check 1 string is equal to other string.
Ans:
if [ "abc" = "abc" ]
then
        echo "String 1 is equal to string 2"
else 
        echo "String 1 is not equal to String 2"
fi
RUN: Check script has execute permission.
chmod +x check_string_equal.sh 
./check_string_equal.sh 
String 1 is equal to string 2

			OR   (by passing two string and check string are equal or not)

if [ $1 = $2 ]
then
        echo "String 1 is equal to string 2"
else 
        echo "String 1 is not equal to String 2"
fi

RUN: pass two string.
./check_string_equal.sh abc abc
output:
String 1 is equal to string 2

 ./check_string_equal.sh abc axc
String 1 is not equal to String 2
----------------------------------------------------------------------------------------------
Q2.Write a script to check 1 string is not equal to other string. using not equal to operator.
Way-1).
-----------------------------------------------------------------------------------------------
if [ "abc" != "abc" ]
then
        echo "String 1 is not equal to string 2"
else 
        echo "String 1 is  equal to String 2"
fi
-----------------------------------------------------------------------------------------------
Run:
./check_string_equal.sh 
String 1 is  equal to String 2
-----------------------------------------------------------------------------------------------
Way-2).
if [ $1 != $2 ]  
then
        echo "String 1 is not equal to string 2"
else 
        echo "String 1 is  equal to String 2"
fi
-----------------------------------------------------------------------------------------------
Run:
bob@caleston-lp10:~$ ./check_string_equal.sh abc abc
String 1 is  equal to String 2
bob@caleston-lp10:~$ ./check_string_equal.sh abc abv
String 1 is not  equal to string 2
------------------------------------------------------------------------------------------------------------------
Q6. whether a command-line argument is not provided using -z
Ans: save script check-command-line-argument.sh
-------------------------------------------------------------------------------------------------------------------
if [ -z "$1" ]; then
    echo "No argument provided."
    exit 1
fi
echo "Argument provided: $1"
--------------------------------------------------------------------------------------------------------------------
Run: 
$ ./script.sh
No argument provided.

$ ./script.sh hello
Argument provided: hello
-------------------------------------------------------------------------------------------------------------------

Q3. Write a script to check 1 string is not equal to other string.
Ans:



------------------------------------------------------------------------------------------------

